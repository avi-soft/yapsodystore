/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "yaptestaddon4.staging.yapsody.com",
      "s3.amazonaws.com",
      "img.icons8.com",
      "img.youtube.com",
    ],
  },
};

export default nextConfig;
